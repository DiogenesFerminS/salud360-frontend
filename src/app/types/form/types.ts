export type AlertType = {
    error: boolean;
    msg: string;
    isActive: boolean;
};

export type FormType ={
    name: string,
    lastname: string,
    email: string,
    phone: string,
    password: string,
};
